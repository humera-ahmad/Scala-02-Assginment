package org.knoldus.lastElement

object LastElement_Impl {
  def main(args: Array[String]): Unit = {
    val list: List[Int] = List()

    val findLastElement=new FindLastElement

    findLastElement.LastElement(list)

  }
}
